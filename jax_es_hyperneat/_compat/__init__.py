"""Internal compatibility shim.

The support modules the algorithm imports (the abstract algorithm base, a config
manager, the TensorNEAT adapter/config generator, schema models, and JAX environment
helpers), packaged so the algorithm runs standalone.

This is an internal implementation detail, not a public API.
"""
